const { Router } = require("express");
const { auth, redirect } = require("../controllers/call.controller");
const router = Router();

router;
//.get("/", redirect)

// get authentictation for the channel;

module.exports = router;
